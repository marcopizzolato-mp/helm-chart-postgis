apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgis-chart.fullname" . }}
  labels:
    {{- include "postgis-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgis-chart.selectorLabels" . | nindent 4 }}
