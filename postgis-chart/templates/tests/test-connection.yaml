apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgis-chart.fullname" . }}-test-connection"
  labels:
    {{- include "postgis-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgis-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
